plugins {
    id 'com.android.application' version '7.4.2'
}

android {
    namespace 'com.nexa.demo'
    compileSdk 34

    defaultConfig {
        applicationId "com.nexa.demo"
        minSdk 24
        targetSdk 34
    }
}

dependencies {
    // 暂时移除Nexa SDK依赖
}
